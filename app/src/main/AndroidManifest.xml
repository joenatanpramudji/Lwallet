<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lwallet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lwallet">
        <activity android:name=".Profile"></activity>
        <activity android:name=".History" />
        <activity android:name=".EnterAmount" />
        <activity android:name=".Destination" />
        <activity android:name=".Success" />
        <activity android:name=".PinConfirmation" />
        <activity android:name=".TopUp" />
        <activity android:name=".MainMenu" />
        <activity android:name=".Register" />
        <activity
            android:name=".cancelled"
            android:label="@string/title_activity_main_menu" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>